// Swap defaults. Canonical SDK form (`XLM.XLM`, `XLM.CODE-ISSUER`); the SDK also accepts the
// Horizon spellings (`native`, `CODE:ISSUER`) — they normalize to the same identifier.
export const USDC = 'XLM.USDC-GA5ZSEJYB37JRC5AVCIA5MOP4RHTM335X2KGX3IHOJAPP5RE34K4KZVN'
export const XLM = 'XLM.XLM'
